name:  GitHub Actions Demo

on: push

jobs:
 build:
  runs-on: Ubuntu-latest

  steps:
   - name: Checkout repository
     uses: actions/checkout@v4

   - name: List files
     run: ls -ltr










